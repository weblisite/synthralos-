# Bandit security linter configuration

[bandit]
# Exclude test files and migrations from security checks
exclude_dirs = tests,alembic,venv,.venv,__pycache__
# Include only high and medium severity issues
severity_level = medium
confidence_level = medium
# Skip certain tests that are false positives
skips = B101,B601
